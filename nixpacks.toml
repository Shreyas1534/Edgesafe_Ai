[phases.setup]
aptPkgs = [
    "libgl1-mesa-glx", 
    "libglib2.0-0", 
    "libsm6", 
    "libxrender1", 
    "libxext6"
]

[phases.build]
cmds = [
    "/app/.venv/bin/pip uninstall -y opencv-python",
    "/app/.venv/bin/pip install opencv-python-headless --force-reinstall"
]